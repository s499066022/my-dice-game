# DnD 工具箱 — 项目改版任务

> 目标：把当前"角色卡 / 团 / 先攻骰 / 地图"四个相对独立的工具，改造成**一间带团战斗室**：一个 DM 带队、N 个玩家各持一张角色卡，围绕一张实时同步的六边形战斗地图，完成"拉人 → 掷先攻 → 排列进场 → 正式战斗"的完整流程。用 **Laravel Reverb 长连接**解决当前"轮询同步不稳定、需刷新才更新"的问题。
>
> 本文档先定架构与分工，再列任务；**先评审，后动工**。
>
> **后端开发者请直接看 [`API.md`](./API.md)**——全部端点、字段、事件、表结构、实现顺序都以它为准。本文档负责讲清"为什么/流程/权限/验收"。

---

## 一、项目定位

一个**跑团战斗辅助 / 战棋**工具。核心价值是"**让一场战斗（角色 vs 怪物）的站位、先攻、血量、法术区域、距离测量都在一张实时地图上协作完成**"。

- **一个团（Party）**：由一个 **DM** 带队，若干 **玩家** 各自持有自己创建的角色卡。
- **战斗发生在六边形地图上**：1 格 = 5 尺；角色、怪物以圆形 token 表示；圆形大小随**体型占格**。
- **角色能力都在角色卡上**；玩家战斗时查看/使用自己的资源与能力，DM 管理怪物。

---

## 二、现状与问题

当前前端（Vue3 + Vite）已有：`/cards`(角色卡)、`/party`(团)、`/dice`(先攻骰)、`/map`(六边形地图)。后端为 Laravel REST（角色卡/先攻/地图/团 均为整体覆盖接口）。

**主要痛点**
1. **同步难**：地图同步靠"每 5 秒 推送 + 拉取合并"，双方同时编辑会互相覆盖，偶尔需刷新才看到——**不适合多人实时协作**。
2. **功能割裂**：地图、先攻、角色卡彼此独立，没有"一场战斗会话"的整体概念，DM 与玩家的权限边界模糊。
3. **无"战斗会话"状态**：先攻顺序、回合、站位、施法区域、当前行动者，缺少一个集中管理的"当前战斗"实体。

---

## 三、角色模型

| 角色 | 能看到/操作 | 说明 |
|---|---|---|
| **DM（领队）** | 全部：建团、拉玩家角色、**创建怪物**、掷先攻、分配/换位、拖全部 token、管理怪物 HP、放置法术区域、切回合 | 对一个战斗会话有完全控制权 |
| **玩家** | 只能操作**自己的角色**：查看角色卡资源/能力、拖动自己 token、改自己 HP、在自己角色/位置放置法术区域 | **第一版先放宽为"全员可改"**（简化权限，后续再收紧） |

> 玩家角色卡能力由玩家自行管理；怪物**只展示 先攻/AC/HP**，其余由 DM 自行管理（不出现在前端）。

## 三·补：怪物卡数据（第一版只"标记并记录"，图鉴后续再做）

怪物的字段以"怪物卡"为准（下图示例：构装骑士）。第一版把这些字段纳入 `monsters`/`combatants` 数据结构存储即可，**不做怪见图鉴页面**。

| 字段 | 值（示例） | 说明 |
|---|---|---|
| 名称 | 构装骑士 | |
| 体型/种类/阵营 | 中型构装，无阵营 | |
| 先攻 | +5 | 参与先攻计算 |
| AC | 18 | |
| HP | (190) 20d10+80 = 201 | 骰式 + 当前值 |
| 速度 | 0 尺 | |
| 六项属性 | 力20(+9) 敏16(+3) 体18(+8) 智1(-5) 感16(+5) 魅1(-5) | 属性值 + 调整 |
| 技能 | 察觉+5，特技+6 | 有加成的技能 |
| 抗性 | 无 | |
| 免疫 | 毒素；目盲、魅惑、耳聋、力竭、恐慌、麻痹、石化、中毒 | 伤害/状态免疫 |
| 装备 | 盾牌，长剑 | |
| 感官 | 被动察觉15，盲视120尺 | |
| 语言 | 无 | |
| CR | 9 | |
| 动作 | 多重攻击：使用附魔长剑攻击三次；附魔长剑：命中+9，5尺，2D6+5(12) 挥砍+2D6(7) 闪电 | 行动条目 |
| 反应动作 | 准备架势：…AC+3、减半、漩涡扭曲(150尺, DC15)传送、长剑攻击 | 反应条目 |

> 存储建议：`monsters` 表存"可用/参与战斗"字段（先攻/AC/HP/位置/大小/颜色/持有者），以及一个 `payload`(json) 存上述怪物卡完整信息（属性/技能/抗性/动作/反应等），供后续做图鉴/展示。

## 四、战斗流程（核心场景）

1. **建团/拉人**
   - DM 创建团，选入玩家角色卡（`/party` 现状保留）。一个团内每个角色唯一归属。
2. **开战斗会话（DM）**
   - 为某个团创建"战斗会话"，与会者上线后加入该会话的**实时频道**。
   - DM **创建怪物**：名称、先攻加值、AC、HP、位置（先放上场，可后移）。怪物其他能力不展示。
3. **掷先攻**
   - 收集**参战者 = 玩家角色 + 怪物**；按先攻骰规则（d20 + 先攻加值 + 优劣）掷骰，得到**行动顺序**。
   - **先攻顺序在地图旁边列出**（一个"行动顺序"面板，紧邻地图）。每个参战者有自己**优劣**，并根据**装备/特性**得到一定的**修正值**；先攻计算方式与先攻骰页一致（`d20 + 先攻加值(含装备修正) + 优劣`），填入内容相同。
   - DM 可**换位**（有的技能/特性允许两个人互换行动顺序）——提供"交换先攻位次"功能。
   - 定好顺序后，**按先攻顺序把参战者放置到地图**（角色、怪物都被安排到场上位置）。
   - 进入正式战斗，按顺序轮转 **当前行动者**。
4. **正式战斗（在地图上）**
   - **地图上的整体信息（站位/先攻/HP/法术区域/回合）全员实时同步**；第一版**全员可改**。
   - **距离尺子**：点两点量距离（格/尺）。
   - **法术区域**：锥形 / 圆形，在地图上做展示；**可绑定到某个 token（追随其移动）**。
   - **移动**：拖拽 token 到目标格（吸附六边形）。
   - **扣血**：玩家扣自己角色血（同步到角色卡），DM 扣怪物血。
   - **查看资源/能力**：玩家点开自己的角色卡面板看资源/能力用法。

---

## 五、技术方案：Laravel Reverb 长连接

用 **Laravel Reverb（内置 WebSocket 服务器）+ laravel-echo 前端客户端** 替换"轮询式"同步。

- **频道**：每个战斗会话一条频道 `presence-combat.{sessionId}`（带在线成员：DM + 玩家），或 `private-combat.{sessionId}`。
- **广播事件**：状态变更由 Laravel 通过 `Broadcast::to(...)` 推送事件；客户端只监听事件更新本地，**不再轮询**。
- **权威服务端**：战斗会话的"当前状态"（顺序、位置、HP、区域、回合）以**服务端为准**（存 DB/Redis），客户端是 renderer；任何修改走 REST 写入 → 服务端广播 → 全员更新。
- **授权（channel authorization）**：DM 频道可改整套会话；普通玩家只能改自己持有者（用 `Authorization` 中间件判断 actor）。

### 建议事件（Broadcast）
| 事件 | 载荷 | 触发 |
|---|---|---|
| `SessionState` | 会话初始化快照（顺序/tokens/区域/回合） | 进频道/入会 |
| `CombatantMoved` | {combatantId, q, r} | 拖拽 token |
| `CombatantHp` | {combatantId, hp:{current,max}} | 扣血/治疗 |
| `InitiativeRolled` | [ {combatantId, order, total} ] | 掷先攻 |
| `CombatantSwapped` | {aId, bId} | 换位 |
| `SpellArea` | {id, type, q,r,angle,ft, boundTo} | 放/改/删法术区域 |
| `TurnChanged` | {currentCombatantId, round} | 切回合 |

### 权限
- 玩家 `*can*`：移动自己的 token、改自己 HP、在自己 token 绑定区域、查看会话。
- DM `*can*`：全部 + 创建怪物 + 掷先攻 + 换位 + 回合管理。服务端按 `session.dm_user_id` 与 token 的 `controlled_by` 校验。

---

## 六、数据模型（后端）

| 实体 | 字段 | 说明 |
|---|---|---|
| `parties` | id, name, dm_user_id, member_ids(json) | 团；每个角色唯一归属 |
| `character_cards` | id, name, payload(json: 完整5e角色卡) | 已有 |
| `monsters` | id, session_id, name, initiative_bonus, ac, hp, color, size, payload(json: 怪物卡完整信息) | 怪物：仅 先攻/AC/HP 互动，其余(json)记录备用 |
| `combat_sessions` | id, party_id, dm_user_id, state(setup/initiative/combat/ended), current_combatant_id, round | 一场战斗的容器 |
| `combatants` | id, session_id, type(character/monster), ref_id, name, color, size, q, r, order, initiative_total, initiative_bonus, advantage(normal/advantage/disadvantage), ac, hp, controlled_by | 参战者（角色或怪物），含先攻信息与优劣 |
| `spell_areas` | id, session_id, type(cone/circle), q, r, angle, ft, bound_to | 法术区域 |

> 说明：`combatants` 是战斗会话内的"参与者"，把"角色/怪物 + 先攻 + 位置 + HP + 优劣"统一起来；先攻顺序（`order`）、回合都挂在 `combatants` 上。`monsters.payload` 存怪物卡完整信息（属性/技能/动作/反应/免疫等，见"三·补"），第一版只记录不展示。

---

## 七、前后端任务拆分

### 后端（Laravel）
1. **基础**
   - 安装/配置 `laravel/reverb` + `laravel-echo` 服务端广播；`composer require laravel/reverb`，`php artisan reverb:start`。
   - 配置 `broadcasting.php`（`REVERB_APP_KEY/SECRET/HOST/PORT`），`config/echo`。
2. **模型+迁移**：`monsters` / `combat_sessions` / `combatants` / `spell_areas`；关系与索引。
3. **REST 接口**（沿用 `ok` 结构、overall 覆盖保留，但新增会话式接口）：
   - `POST /api/combat-sessions`（DM 建会）、`GET /api/combat-sessions/{id}`（快照）。
   - `POST /api/combat-sessions/{id}/roll-initiative`（掷先攻）。
   - `PATCH /api/combatants/{id}`（位置/HP/次序）。
   - `POST /api/combatants/{id}/swap`（换位，发起者临时态→服务端校验）。
   - `POST /api/combat-sessions/{id}/spell-areas`、`PATCH/DELETE .../spell-areas/{id}`。
   - `POST /api/combat-sessions/{id}/turn`（切回合）。
   - 保留 `characters`/`parties`/`map` 现状。
4. **广播**：在写入后 `broadcast(new Combat*Event(...))` 到 `presence-combat.{sessionId}`。
5. **授权中间件**：`session.dm_user_id` + `combatant.controlled_by` 校验。
6. **在线列表**：presence 频道回传成员（DM/玩家）。

### 前端（Vue）
1. **接入 Echo/Reverb**：`laravel-echo` + `pusher-js`、`options.broadcaster='reverb'`，监听 `presence-combat.*`。
2. **战斗会话入口**：`/party` 里 DM 点"开团战斗"→ 建会话；玩家在会话中"以角色进入"。
3. **地图改造（核心）**：
   - 绑定某个 `combat-sessions`；加载 `combatants`（角色/怪物）、`spell_areas`。
   - token 用 `combatant.id` 与 `controlled_by` 区分；**玩家只能拖自己 token**，DM 拖全部。
   - **先攻顺序**在地图/侧栏展示，按 `order` 排列；**当前行动者**高亮。
   - **换位**：DM 选择两个参战者"换位"（POST swap）。
   - **尺子**（量距）、**法术区域**（锥/圆，可绑定 token 跟随）。
   - **扣血**：玩家 token 的 HP 操作（同步角色卡），DM 怪物 HP。
4. **先攻页**：并入战斗会话（掷骰、排序、换位），不再独立轮询。
5. **角色卡**：战斗时玩家点开自己角色 → 查看**资源/能力**（复用 `/cards` 面板，只读/可改自己）。
6. **移动端**：保留触摸拖动。

---

## 八、关键决策（ADR）

1. **Reverb 长连接 vs 轮询**：选 Reverb。理由：多人实时协作、减少覆盖、无需刷新。REST 仍负责"写入 + 初始快照"，广播负责"增量通知"。
2. **服务端权威态**：顺序/位置/HP/区域以**服务端**为准，客户端=Rendered。避免"每 5 秒推拉合并"的覆盖问题。
3. **权限模型**：`DM = 全套`，`玩家 = 仅自己 token/角色`。服务端校验，前端只做呈现。**第一版：先放宽为"全员可改、全员同步"**（后续用 `controlled_by`/`dm_user_id` 收紧）。
4. **先攻与位置都在 `combatants`**：不再让"先攻页"和"地图"各维护一份，统一以会话内的 `combatants` 为准（顺序决定回合，位置决定站位）。

---

## 九、阶段性任务顺序

1. **阶段A（后端骨架）**：Reverb 配置 + 会话/怪物/参战者/法术区模型与迁移 + 建会接口 + 初始快照广播。
2. **阶段B（前端接入）**：Echo 连接到会话频道，收到 `SessionState` 渲染地图；确认实时更新生效。
3. **阶段C（战斗流程）**：掷先攻 → 排序/animate 进场 → 换位 → 回合切换 → 当前行动者高亮。
4. **阶段D（战斗内操作）**：拖拽移动、尺子、法术区域（锥/圆+绑定）、扣血（角色卡联动）。
5. **阶段E（权限/体验）**：DM/玩家权限，玩家只读自己资源面板，移动端优化。

---

## 十、验收标准
- 两个浏览器（一 DM 一 玩家）进入同一战斗会话，一方的拖拽/先攻/扣血/放区域**实时**出现在另一方，无需刷新。
- 玩家只能操作自己的 token；DM 能管全部。
- 按描述跑通：建团拉人 → 建怪物 → 掷先攻 → 换位 → 进场 → 战斗（尺子/法术/移动/扣血/看资源）。

---

## 十一、当前实现状态（前端已完成）与后端对接说明

> **接口契约以 [`API.md`](./API.md) 为准**。本文档描述流程与架构；`API.md` 给出每个端点/事件/表的最终字段与约定（`ok` 结构、CORS、鉴权）。后端开发者实现时对照 `API.md`。

### 前端已完成（可以直接对接）
| 能力 | 现状 | 对接点 |
|---|---|---|
| 团（Party） | `/party` 建团、拉角色；**每个角色唯一归属** | `parties` |
| 战斗会话 | **`会话 id = 团 id`**（选择团即加入该会话）；选择团后自动加载该团数据；"未选择团"为默认空选项 | `combat_sessions`（以 party_id 关联） |
| 参战者 | 角色（来自团成员）+ 怪物；**不叠放**（自动一字排开 `q=i-len/2, r=0`）；`type=character` 且 `ref_id` 已在会话内时**防重复加入** | `combatants.sync` |
| 先攻 | 地图旁"行动顺序"表：次序/类型/名字/优劣/掷骰/修正/最终/AC/HP；`total = d20 + 先攻加值(含装备修正) + 优劣 ±5`；**换位**（交换位次）；**当前行动者高亮**；HP 直接输入 | `roll-initiative` / `swap` / `turn` |
| 地图 | 六边形无限网格（平顶），缩放/平移/触摸；token 随体型缩放；**拖拽移动**；**尺子**量距（格/尺）；**法术区域**（锥/圆）可**绑定到 token 跟随**；**整体信息全量同步** | `combatants` / `spell_areas` |
| 锁定 | 会话 `locked`：**屏蔽删除人物、屏蔽移动/换位**；左上的"移动/删除"图标已按需求隔离（间距放大、删除人物屏蔽） | `combatants` 写接口校验 |
| 防重复 | 会话列表用 `ref_id`（角色卡 id）标记，**同一角色卡不能重复加入** | 服务端 `ref_id` 唯一校验 |

### 前端正在用（当前仅本地 localStorage，等后端上线后切到接口 + Reverb）
- `useCombatSession`(单例) 持有 `sessionId/state/combatants/spellAreas/round/currentCombatantId/locked`，`sessionKey = dnd-combat-session-{sessionId}`，读写本地；`createOrJoinParty(partyId)`、`addCharacter`、`addMonster`、`removeCombatant/moveCombatant/swapCombatants`（`locked` 时拒绝）、`hpCombatant`、`rollInitiative`(排序并重编 order)、`nextRound`、`addSpellArea/removeSpellArea`、`reconcileParty`（按团成员对账，保留怪物）。
- `useCombatSession` 已接入 `connectReverb`（监听 `presence-combat.{channel}` 与 `CombatSessionState/CombatantUpdated/...`），但**尚无 `window.Echo`**（`laravel-echo`/`pusher-js` 未安装），当前 `online=false`、全部退化到本地。
- 前端**只订阅 Reverb，尚未 POST 写入后端**（后端接口未上线，改动用 localStorage 兜底）。

### 后端待办（对照 `API.md` 实现）
1. **基础设施**：安装/配置 `laravel/reverb` + `laravel-echo` 服务端广播；`composer require laravel/reverb`，`php artisan reverb:start`；`broadcasting.php`/`config/echo` 的 `REVERB_APP_*`。
2. **模型 + 迁移**：`combat_sessions` / `combatants` / `spell_areas`（怪物卡复用 `combatants.payload` 或独立 `monsters`，见 `API.md`）。
3. **REST 端点**：`ping`、`characters`、`parties`、`map`（保留现状）+ 全部战斗会话端点（建会/快照/同步/掷先攻/换位/回合/法术区/锁定），**均以 `API.md` 的字段与 `ok` 结构为准**。
4. **广播**：各写接口完成后 `broadcast(new Combat*Event(...))` 到 `presence-combat.{sessionId}`；事件名与载荷见 `API.md` `Reverb 事件` 表。
5. **鉴权**：`session.dm_user_id` + `combatant.controlled_by`（第一版服务端可先放宽，前端已默认"全员可改"）。
6. **在线列表**：presence 频道回传 DM/玩家成员。

### 前端后续（后端接口可用后）
- 安装 `laravel-echo` + `pusher-js`，`initEcho()` 连接，`useCombatSession` 把写入改为 POST 到 `API.md` 端点，并订阅广播增量更新（不再依赖 localStorage 兜底）。
- 先攻定序后**一键按顺序进场**（当前已手动一字排开，可加"按次序自动摆位"）。

---

## 十二、后端实现对照清单（速查）
见 [`API.md`](./API.md) 的"实现顺序"小节，按 基础 → 数据表 → 各资源端点 → 战斗会话端点 → Reverb 事件 → 鉴权 顺序完成即可。
